{
  "schema_version": "emboss-rs.autodoc/v1",
  "document_id": "charge-v1",
  "tool": {
    "name": "charge",
    "family": "protein_plots",
    "summary": "Report a sliding-window protein charge profile and emit a line-plot contract",
    "legacy_names": [
      "charge"
    ]
  },
  "sections": [
    {
      "id": "overview",
      "kind": "overview",
      "title": "Overview",
      "content": "`charge` is the first production plotting vertical slice in EMBOSS-RS. It was chosen because the analytical output is narrow, deterministic, and plot-ready without hidden biological heuristics: one protein input produces a sliding-window numeric profile with a single numeric x axis and y axis."
    },
    {
      "id": "inputs",
      "kind": "inputs",
      "title": "Inputs",
      "content": "This tool accepts exactly one protein sequence record. v1 uses a fixed residue-charge model in Rust, requires protein-compatible residues, and treats the window and step arguments as positive counts over the original sequence."
    },
    {
      "id": "outputs",
      "kind": "outputs",
      "title": "Outputs",
      "content": "The implementation emits two coordinated outputs: a structured analytical table of window starts, ends, lengths, and mean charges, plus a typed line-plot contract payload. The plot contract is consumed by the sister `emboss-r` package, which owns rendering."
    },
    {
      "id": "plotting",
      "kind": "notes",
      "title": "Plotting Integration",
      "content": "Rust does not render figures. The formal contract emitted by `charge` is the canonical handoff to R. In `emboss-r`, `charge_profile()` returns a structured result object carrying both the analytical data frame and the parsed plot contract, and `plot()` or `render_charge_plot()` renders the governed line plot with `ggplot2`."
    },
    {
      "id": "status",
      "kind": "notes",
      "title": "Current Status",
      "content": "This method is implemented and exposed through `emboss-rs charge`. Validation now covers stable analytical rows plus compared acceptance evidence for the canonical checked-in line-plot contract fixture, and the R package consumes the same contract for the first production end-to-end plotting path."
    },
    {
      "id": "caveats",
      "kind": "caveats",
      "title": "Caveats",
      "content": "v1 supports the charge profile only for one protein record at a time. Rendering remains intentionally R-owned, so non-graphical Rust and CLI workflows remain usable without R. Additional plot-capable methods should follow this contract-and-renderer pattern rather than adding Rust-side graphics code."
    }
  ],
  "artifacts": [
    {
      "id": "charge_fixture",
      "label": "Protein fixture for governed charge validation",
      "origin": {
        "kind": "fixture_asset"
      },
      "acquisition": {
        "mode": "fixture"
      },
      "reference": {
        "kind": "managed_asset",
        "asset_id": "crates/emboss-tools/tests/fixtures/charge_protein.fasta"
      },
      "description": "Repository-managed protein fixture used for deterministic charge-profile validation."
    },
    {
      "id": "charge_plot_contract",
      "label": "Canonical charge line-plot contract fixture",
      "origin": {
        "kind": "fixture_asset"
      },
      "acquisition": {
        "mode": "fixture"
      },
      "reference": {
        "kind": "managed_asset",
        "asset_id": "crates/emboss-tools/tests/fixtures/charge_plot_contract.json"
      },
      "description": "Repository-managed canonical line-plot contract fixture emitted by the governed charge implementation."
    }
  ],
  "examples": [
    {
      "id": "charge_profile_example",
      "title": "Compute a deterministic protein charge profile and emit a line-plot contract",
      "description": "Reports deterministic sliding-window charge rows from the committed protein fixture and emits the canonical governed line-plot contract.",
      "artifact_ids": [
        "charge_fixture",
        "charge_plot_contract"
      ],
      "parameters": [
        {
          "name": "window",
          "value": "5"
        },
        {
          "name": "step",
          "value": "1"
        }
      ],
      "expected_outputs": [
        {
          "id": "charge_profile_table",
          "label": "Charge profile table",
          "description": "Stable sliding-window charge rows plus a canonical line-plot contract derived from the same governed output."
        },
        {
          "id": "charge_plot",
          "label": "Charge line-plot contract",
          "description": "The canonical governed line-plot contract JSON emitted from the same deterministic analytical run."
        }
      ]
    }
  ],
  "provenance": {
    "source_mode": "curated",
    "curated_by": "emboss-rs maintainers",
    "source_references": [
      {
        "source": "EMBOSS charge application",
        "locator": "https://github.com/kimrutherford/EMBOSS/blob/master/emboss/acd/charge.acd",
        "invocation": "charge -sequence charge_protein.fasta -winsize 5 -graph data"
      }
    ]
  },
  "validation": {
    "required_example_ids": [
      "charge_profile_example"
    ],
    "compare_against_legacy": true,
    "require_provenance_capture": true
  }
}
