{
  "schema_version": "emboss-rs.autodoc/v1",
  "document_id": "biosed-v1",
  "tool": {
    "name": "biosed",
    "family": "sequence_edit",
    "summary": "Replace or delete explicit sequence intervals record by record",
    "legacy_names": [
      "biosed"
    ]
  },
  "sections": [
    {
      "id": "overview",
      "kind": "overview",
      "title": "Overview",
      "content": "`biosed` performs bounded interval editing over each sequence record in one local input. The EMBOSS-RS v1 surface supports 1-based inclusive deletion or replacement of the same interval across all records and emits a cleaned FASTA stream rather than free-form text."
    },
    {
      "id": "inputs",
      "kind": "inputs",
      "title": "Inputs",
      "content": "The current interface accepts one local sequence input plus explicit `start` and `end` coordinates. Supplying `--replace <residues>` inserts replacement residues; omitting the flag deletes the selected interval. Coordinates must remain in range for every record."
    },
    {
      "id": "outputs",
      "kind": "outputs",
      "title": "Outputs",
      "content": "The tool emits edited FASTA records in input order. CLI summaries report the source input, edited interval, replacement or delete mode, coordinate convention, and the annotation-drop caveat."
    },
    {
      "id": "status",
      "kind": "notes",
      "title": "Current Status",
      "content": "This method is implemented and exposed through `emboss-rs biosed`. Validation currently covers deterministic interval replacement and deletion against committed FASTA fixtures in the Rust tool and service layers."
    },
    {
      "id": "caveats",
      "kind": "caveats",
      "title": "Caveats",
      "content": "The v1 implementation applies the same coordinates to every record, drops feature annotations after editing, and does not support regex-style edits, multiple independent edit operations in one invocation, or annotation-coordinate remapping."
    }
  ],
  "artifacts": [
    {
      "id": "biosed_records_fasta",
      "label": "Biosed FASTA fixture",
      "origin": {
        "kind": "fixture_asset"
      },
      "acquisition": {
        "mode": "fixture"
      },
      "reference": {
        "kind": "managed_asset",
        "asset_id": "crates/emboss-tools/tests/fixtures/biosed_records.fasta"
      },
      "description": "Repository-managed FASTA input fixture used for deterministic interval editing validation."
    }
  ],
  "examples": [
    {
      "id": "replace_interval_across_records",
      "title": "Replace a shared interval in every input record",
      "description": "Runs `biosed` against the committed FASTA fixture and replaces residues 2..3 with `NN` in every record.",
      "artifact_ids": [
        "biosed_records_fasta"
      ],
      "parameters": [],
      "expected_outputs": [
        {
          "id": "biosed_output_records",
          "label": "Edited sequence records",
          "description": "The output records are emitted as FASTA with residues `ANNG` and `TNNN`, and the shared result summary reports `Start: 2`, `End: 3`, and `Replacement: NN`."
        }
      ],
      "legacy_reference": {
        "source": "EMBOSS biosed application",
        "locator": "https://github.com/kimrutherford/EMBOSS/blob/master/emboss/acd/biosed.acd",
        "invocation": "biosed -sequence biosed_records.fasta -start 2 -end 3 -replace NN -outseq stdout"
      }
    }
  ],
  "provenance": {
    "source_mode": "curated",
    "curated_by": "emboss-rs maintainers",
    "source_references": []
  },
  "validation": {
    "required_example_ids": [
      "replace_interval_across_records"
    ],
    "compare_against_legacy": false,
    "require_provenance_capture": true
  }
}
