{
  "schema_version": "epithema.autodoc/v1",
  "document_id": "complex-v1",
  "tool": {
    "name": "complex",
    "family": "sequence_stats",
    "summary": "Report whole-sequence and sliding-window nucleotide linguistic complexity",
    "legacy_names": [
      "complex"
    ]
  },
  "sections": [
    {
      "id": "overview",
      "kind": "overview",
      "title": "Overview",
      "content": "`complex` reports nucleotide linguistic complexity over an inclusive k-mer range using the shared Epithema complexity core. The first release computes one whole-sequence complexity row per input record and can additionally emit sliding-window rows when `--window` and `--step` are supplied together."
    },
    {
      "id": "inputs",
      "kind": "inputs",
      "title": "Inputs",
      "content": "The current tool accepts one local nucleotide input plus required `--k-min` and `--k-max` values. Sliding-window mode additionally requires both `--window` and `--step`. The v1 implementation is strict A/C/G/T only and rejects unsupported or ambiguous symbols instead of approximating them."
    },
    {
      "id": "outputs",
      "kind": "outputs",
      "title": "Outputs",
      "content": "The tool emits a stable table report. Whole-sequence rows use `scope=record`, while optional sliding-window rows use `scope=window` with deterministic 1-based inclusive start and end coordinates."
    },
    {
      "id": "status",
      "kind": "notes",
      "title": "Current Status",
      "content": "This method is implemented and exposed through `epithema complex`. Validation currently covers deterministic whole-sequence complexity reporting on a committed nucleotide fixture, and Rust tool and service tests also exercise sliding-window output and unsupported-symbol rejection."
    },
    {
      "id": "caveats",
      "kind": "caveats",
      "title": "Caveats",
      "content": "The retained exception `complex` remains intentionally narrow in v1. It does not accept ambiguous residues, does not implement richer alphabet models, and does not attempt cross-record comparative summaries beyond the emitted per-record and optional per-window rows."
    }
  ],
  "artifacts": [
    {
      "id": "complex_records_fasta",
      "label": "Two-record nucleotide complexity fixture",
      "origin": {
        "kind": "fixture_asset"
      },
      "acquisition": {
        "mode": "fixture"
      },
      "reference": {
        "kind": "managed_asset",
        "asset_id": "crates/epithema-tools/tests/fixtures/complex_records.fasta"
      },
      "description": "Repository-managed FASTA fixture with low- and high-complexity nucleotide examples for deterministic complexity reporting."
    }
  ],
  "examples": [
    {
      "id": "report_whole_sequence_complexity",
      "title": "Report whole-sequence nucleotide complexity over k=1..2",
      "description": "Runs `complex` against a committed two-record nucleotide FASTA fixture and emits stable whole-sequence complexity rows.",
      "artifact_ids": [
        "complex_records_fasta"
      ],
      "parameters": [
        {
          "name": "--k-min",
          "value": "1"
        },
        {
          "name": "--k-max",
          "value": "2"
        }
      ],
      "expected_outputs": [
        {
          "id": "complexity_table",
          "label": "Whole-sequence complexity table",
          "description": "A stable report containing one `record` row for the low-complexity fixture sequence and one `record` row for the higher-complexity fixture sequence."
        }
      ],
      "legacy_reference": null
    }
  ],
  "provenance": {
    "source_mode": "curated",
    "curated_by": "epithema maintainers",
    "source_references": []
  },
  "validation": {
    "required_example_ids": [
      "report_whole_sequence_complexity"
    ],
    "compare_against_legacy": false,
    "require_provenance_capture": true
  }
}
