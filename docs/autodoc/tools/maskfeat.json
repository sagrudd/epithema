{
  "schema_version": "epithema.autodoc/v1",
  "document_id": "maskfeat-v1",
  "tool": {
    "name": "maskfeat",
    "family": "feature_tools",
    "summary": "Mask selected simple annotated feature spans in-place while preserving record annotations",
    "legacy_names": [
      "maskfeat"
    ]
  },
  "sections": [
    {
      "id": "overview",
      "kind": "overview",
      "title": "Overview",
      "content": "`maskfeat` masks sequence spans defined by selected annotated features from EMBL or GenBank inputs using the shared Epithema feature-selection and interval-masking path. The original record is retained, matching simple feature spans are masked in place, and feature annotations remain attached to the resulting payload."
    },
    {
      "id": "inputs",
      "kind": "inputs",
      "title": "Inputs",
      "content": "The current v1 tool accepts annotated EMBL or GenBank input plus optional selector flags. Supported selectors are `--kind`, `--name`, `--qualifier`, and `--strand`. When more than one selector is supplied they are combined conjunctively. If no selector flags are provided, all features in each input record are selected."
    },
    {
      "id": "outputs",
      "kind": "outputs",
      "title": "Outputs",
      "content": "The output preserves source record order and emits masked full-length records in FASTA form while retaining original feature annotations in the structured payload. Overlapping or adjacent selected simple spans are masked deterministically in place, so repeated coverage does not change the final result beyond replacing covered residues with the chosen mask symbol."
    },
    {
      "id": "masking",
      "kind": "notes",
      "title": "Masking Policy",
      "content": "The default mask symbol is `N` for nucleotide and unknown-molecule records and `X` for protein records. A custom single-character mask can be supplied with `--mask-char` only when that symbol is valid for the target alphabet. Selected feature spans are not removed or rebased; they remain attached to the original coordinate system of the masked record."
    },
    {
      "id": "status",
      "kind": "notes",
      "title": "Current Status",
      "content": "This method is implemented and exposed through `epithema maskfeat`. Validation currently covers masking a selected gene feature from a committed annotated GenBank fixture. Rust service and core tests also cover masking all selected spans in one record, no-match handling, invalid mask-symbol handling, and conservative failure on unsupported complex feature locations."
    },
    {
      "id": "caveats",
      "kind": "caveats",
      "title": "Caveats",
      "content": "The v1 scope supports only simple single-span feature locations. Joined, compound, or otherwise complex locations fail clearly rather than being partially masked. If no selected features are found across the input, the tool fails with an explicit validation error instead of silently returning unchanged records."
    }
  ],
  "artifacts": [
    {
      "id": "annotated_feature_genbank",
      "label": "Annotated GenBank fixture",
      "origin": {
        "kind": "fixture_asset"
      },
      "acquisition": {
        "mode": "fixture"
      },
      "reference": {
        "kind": "managed_asset",
        "asset_id": "crates/epithema-tools/tests/fixtures/annotated_feature.gbk"
      },
      "description": "Repository-managed annotated GenBank fixture used for deterministic maskfeat validation."
    }
  ],
  "examples": [
    {
      "id": "mask_selected_gene_feature",
      "title": "Mask a selected gene feature in-place on an annotated record",
      "description": "Selects the `gene` feature from a small annotated GenBank fixture and masks that span with the default nucleotide mask symbol while preserving the source record length and annotations.",
      "artifact_ids": [
        "annotated_feature_genbank"
      ],
      "parameters": [
        {
          "name": "kind",
          "value": "gene"
        }
      ],
      "expected_outputs": [
        {
          "id": "feature_masked_sequences",
          "label": "Feature-masked sequence records",
          "description": "The source sequence is returned at full length with the selected feature span replaced by masking symbols in place."
        }
      ],
      "legacy_reference": {
        "source": "EMBOSS maskfeat application",
        "locator": "https://github.com/kimrutherford/EMBOSS/blob/master/emboss/acd/maskfeat.acd",
        "invocation": "maskfeat -sequence annotated_feature.gbk -type gene -outseq stdout"
      }
    }
  ],
  "provenance": {
    "source_mode": "curated",
    "curated_by": "epithema maintainers",
    "source_references": []
  },
  "validation": {
    "required_example_ids": [
      "mask_selected_gene_feature"
    ],
    "compare_against_legacy": false,
    "require_provenance_capture": true
  }
}
