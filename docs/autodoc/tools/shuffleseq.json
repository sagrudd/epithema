{
  "schema_version": "emboss-rs.autodoc/v1",
  "document_id": "shuffleseq-v1",
  "tool": {
    "name": "shuffleseq",
    "family": "sequence_transform",
    "summary": "Shuffle sequence residues deterministically while preserving composition",
    "legacy_names": [
      "shuffleseq"
    ]
  },
  "sections": [
    {
      "id": "overview",
      "kind": "overview",
      "title": "Overview",
      "content": "`shuffleseq` permutes the residues within each input record while preserving exact per-record composition. The EMBOSS-RS v1 implementation is deterministic by design: when `--seed` is omitted it uses a fixed documented base seed, and each record derives its own deterministic stream from that base seed and input order."
    },
    {
      "id": "inputs",
      "kind": "inputs",
      "title": "Inputs",
      "content": "The current interface accepts one local sequence input plus an optional `--seed`. Multi-record inputs are supported. Record order is preserved, and each record is shuffled independently rather than pooling residues across the full input stream."
    },
    {
      "id": "outputs",
      "kind": "outputs",
      "title": "Outputs",
      "content": "The tool emits one shuffled FASTA sequence collection. CLI summaries report the source input, the base seed used, the deterministic per-record permutation rule, exact composition preservation, and FASTA output format."
    },
    {
      "id": "status",
      "kind": "notes",
      "title": "Current Status",
      "content": "This method is implemented and exposed through `emboss-rs shuffleseq`. Validation currently covers deterministic seeded shuffling and composition preservation against a committed multi-record FASTA fixture, and Rust service tests exercise the same repository-managed case."
    },
    {
      "id": "caveats",
      "kind": "caveats",
      "title": "Caveats",
      "content": "The first release does not offer cryptographic randomness, cross-record shuffling, or feature-coordinate preservation. It preserves identifiers and metadata, but any attached feature coordinates should not be treated as biologically meaningful after shuffling."
    }
  ],
  "artifacts": [
    {
      "id": "three_record_fasta",
      "label": "Three-record FASTA fixture",
      "origin": {
        "kind": "fixture_asset"
      },
      "acquisition": {
        "mode": "fixture"
      },
      "reference": {
        "kind": "managed_asset",
        "asset_id": "crates/emboss-tools/tests/fixtures/three_records.fasta"
      },
      "description": "Repository-managed three-record FASTA fixture used for deterministic shuffleseq validation."
    }
  ],
  "examples": [
    {
      "id": "shuffle_records_with_seed_7",
      "title": "Shuffle a three-record FASTA fixture with an explicit seed",
      "description": "Runs `shuffleseq` against the committed three-record FASTA fixture with `--seed 7` and returns deterministic per-record permutations that preserve composition exactly.",
      "artifact_ids": [
        "three_record_fasta"
      ],
      "parameters": [
        {
          "name": "--seed",
          "value": "7"
        }
      ],
      "expected_outputs": [
        {
          "id": "shuffled_sequence_collection",
          "label": "Deterministically shuffled sequence collection",
          "description": "A FASTA sequence collection whose first record residues are `CGTA`, while overall per-record composition remains unchanged."
        }
      ],
      "legacy_reference": null
    }
  ],
  "provenance": {
    "source_mode": "curated",
    "curated_by": "emboss-rs maintainers",
    "source_references": []
  },
  "validation": {
    "required_example_ids": [
      "shuffle_records_with_seed_7"
    ],
    "compare_against_legacy": false,
    "require_provenance_capture": true
  }
}
