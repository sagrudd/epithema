{
  "schema_version": "emboss-rs.autodoc/v1",
  "document_id": "trimseq-v1",
  "tool": {
    "name": "trimseq",
    "family": "sequence_edit",
    "summary": "Trim explicit residue counts from the left and right ends of sequence records",
    "legacy_names": [
      "trimseq"
    ]
  },
  "sections": [
    {
      "id": "overview",
      "kind": "overview",
      "title": "Overview",
      "content": "`trimseq` removes an explicit number of residues from the left and right ends of each input record. The EMBOSS-RS v1 implementation keeps the model narrow and deterministic: trimming is by residue count, applied uniformly to every record, and rejected if the requested total would exhaust a sequence."
    },
    {
      "id": "inputs",
      "kind": "inputs",
      "title": "Inputs",
      "content": "The current tool accepts one local sequence input plus optional `--left` and `--right` counts. Counts are non-negative and default to zero. Multi-record inputs are supported, and the same trim counts are applied to every record in input order."
    },
    {
      "id": "outputs",
      "kind": "outputs",
      "title": "Outputs",
      "content": "Output is a normalized sequence collection rendered through the shared result and CLI layers. Trimmed records retain their original identifiers, descriptions, and molecule metadata when the resulting sequence remains valid."
    },
    {
      "id": "status",
      "kind": "notes",
      "title": "Current Status",
      "content": "This method is implemented and exposed through `emboss-rs trimseq`. Validation currently covers representative left-and-right trimming against a committed FASTA fixture, while Rust service tests cover deterministic output ordering and exhaustion rejection."
    },
    {
      "id": "caveats",
      "kind": "caveats",
      "title": "Caveats",
      "content": "The v1 scope trims only fixed residue counts and does not implement motif-based clipping, adapter discovery, or quality-aware trimming. If `--left` plus `--right` would remove all residues from a record, the tool fails rather than emitting an empty sequence."
    }
  ],
  "artifacts": [
    {
      "id": "three_record_fasta",
      "label": "Three-record FASTA fixture",
      "origin": {
        "kind": "fixture_asset"
      },
      "acquisition": {
        "mode": "fixture"
      },
      "reference": {
        "kind": "managed_asset",
        "asset_id": "crates/emboss-tools/tests/fixtures/three_records.fasta"
      },
      "description": "Repository-managed FASTA fixture used for deterministic trimseq validation."
    }
  ],
  "examples": [
    {
      "id": "trim_one_residue_from_each_end",
      "title": "Trim one residue from the left and right ends of each record",
      "description": "Runs `trimseq` against the committed three-record FASTA fixture with one residue removed from each end of every input record.",
      "artifact_ids": [
        "three_record_fasta"
      ],
      "parameters": [
        {
          "name": "--left",
          "value": "1"
        },
        {
          "name": "--right",
          "value": "1"
        }
      ],
      "expected_outputs": [
        {
          "id": "trimmed_sequences",
          "label": "Trimmed sequence output",
          "description": "Stable FASTA output in which `alpha` becomes `CG`, `beta` becomes `TT`, and `gamma` becomes `GC`."
        }
      ],
      "legacy_reference": null
    }
  ],
  "provenance": {
    "source_mode": "curated",
    "curated_by": "emboss-rs maintainers",
    "source_references": []
  },
  "validation": {
    "required_example_ids": [
      "trim_one_residue_from_each_end"
    ],
    "compare_against_legacy": false,
    "require_provenance_capture": true
  }
}
