# aaindexextract hydropathy subset case

This governed case exercises the built-in `hydropathy_kyte_doolittle` index
for `aaindexextract` and confirms that Epithema emits the stable residue table
for its curated v1 subset rather than claiming full historical AAINDEX parity.
