# makeprotseq governed case

This fixture records the governed `makeprotseq` example that generates one
canonical protein record from a deterministic seed. It keeps the generated-case
evidence explicit and repository-managed for the first-release surface.
