# makenucseq governed case

This fixture records the governed `makenucseq` example that generates two RNA
records from a deterministic seed. It exists so the autodoc contract can point
to a repository-managed case note rather than pretending a historical EMBOSS
artefact exists for this bounded v1 generation surface.
